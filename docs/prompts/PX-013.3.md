# PX-013.3 — Automation Insights & Queue Preview
**Targets:** RM-013  
**Date:** 2025-11-09  
**Outcome:** Draft  
**Design Reference:** Align implementation with `docs/analysis/IdleGameComparative.md`.

## Driver Prompt
- Extend the Automation HUD to show queue depth, per-routine cadence, and burst windows so players can trust Auto Feeders at a glance.  
- Surface automation alerts (power throttles, conveyor jams) as inline badges that mirror StatBus state without forcing tab swaps.  
- Add controller + keyboard shortcuts for pausing automation globally and for cycling routines while the automation sheet is open.  
- Record automation queue depth and cadence deltas in StatsProbe so telemetry dashboards can correlate automation drift with power issues.  
- Update DocOps: `docs/modules/automation.md`, `docs/architecture/StatBus_Catalog.md`, and HUD copy tables need new strings/tooltips for the alerts.

## Deliverables
- Updated automation scene scripts (prototype HUD + `scenes/ui/panels/AutomationPanel.*`) with queue/badge UI and input bindings.  
- AutomationService / StatBus extensions exposing queue depth, burst windows, and alert states.  
- Docs refreshed per Driver Prompt plus changelog/roadmap notes.

## Validation
- `python3 tools/docs_lint/check_structure.py`  
- `$(bash tools/godot_resolver.sh) --headless --script res://tools/uilint_scene.gd -- res://scenes/ui_smoke/MainHUD.tscn`  
- `PSEUDO_LOC=1 ./tools/pseudo_loc_smoke.sh`  
- `$(bash tools/godot_resolver.sh) --headless --script res://tools/replay_headless.gd --duration=120 --seed=42 --strategy=idle` (attach StatsProbe CSV slice highlighting new fields)  
- HUD controller sweep per `docs/qa/RM-010-ui-checklist.md`
